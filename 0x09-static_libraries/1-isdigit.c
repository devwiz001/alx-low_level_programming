#include "main.h"
/**
 * _isdigit - checks for a digit from 0 to 9
 *
 * @c: integer to be checked
 *
 * Return: 1 if c is a number otherwise 0
 *
 */

int _isdigit(int c)

{

	return (c >= '0' && c <= '9');

}
