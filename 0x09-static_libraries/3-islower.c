
#include "main.h"

/**
 * _islower - it checks for lowercase character
 *
 * @c: the character to check
 *
 * Return: 1 if c is lowercase, return 0 otgerwise
 *
 */

int _islower(int c)

{

	return (c >= 'a' && c <= 'z');

}
