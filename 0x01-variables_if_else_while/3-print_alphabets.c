#include <stdio.h>
/**
 * main - void
 * Return: 0 (always successful)
 */

int main(void)
{
	char a;

	char b;

	a = 'c';
	b = 'C';
	while
		(a <= 'd') {
			putchar(a);
			a++;
		}
	while
		(b <= 'D') {
			putchar (b);
			b++;
		}
	putchar('\n');
	return (0);
}
